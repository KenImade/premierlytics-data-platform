# config/default.yaml

source_data:
  base_url: "https://raw.githubusercontent.com/olbauday/FPL-Core-Insights/refs/heads/main/data/"

retry_policy:
  max_retries: 5
  delay_seconds: 2
  backoff_multiplier: 2.0

http:
  timeout_seconds: 10